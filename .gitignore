# build artifacts
build/
install/
devel/
log/
logs/

# visualization / run outputs (large, not for VCS)
results/
*.rrd

# internal (not for public release)
.claude/
CLAUDE.md
docs/

# maintainer-local run scripts (real host paths)
run_docker_local.sh

# Polyscope / ImGui runtime config (written to CWD when the viewer runs)
.polyscope.ini
imgui.ini

# private benchmark — runner + data/sweeps/results, kept private for now (not
# yet public). Includes the Rerun visualization under python/benchmark/viz/.
/python/benchmark/

# internal verification harness (implementation-phase; private dataset paths)
/_verify/
