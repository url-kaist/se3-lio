# build artifacts
build/
install/
devel/
log/
logs/

# visualization / run outputs (large, not for VCS)
results/
*.rrd

# internal (not for public release)
.claude/
CLAUDE.md
docs/

# maintainer-local run scripts (real host paths)
run_docker_local.sh

# Polyscope / ImGui runtime config (written to CWD when the viewer runs)
.polyscope.ini
imgui.ini

# private benchmark data/results
/benchmark/

# python benchmark runner + its tests — kept private for now (not yet public).
# Includes the Rerun visualization (moved under benchmark/viz/), also private.
/python/se3_lio/benchmark/
/python/tests/_bench_import.py
/python/tests/test_benchmark_evaluate.py
/python/tests/test_benchmark_search.py
/python/tests/test_rerun_viz.py

# internal verification harness (implementation-phase; private dataset paths)
/_verify/
