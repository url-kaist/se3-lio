# build artifacts
build/
install/
devel/
log/
logs/

# visualization / run outputs (large, not for VCS)
results/
*.rrd

# internal (not for public release)
.claude/
CLAUDE.md
docs/

# maintainer-local run scripts (real host paths)
run_docker_local.sh

# private benchmark data/results (templates live under python/se3_lio/benchmark/examples/)
/benchmark/
